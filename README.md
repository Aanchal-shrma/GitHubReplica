A full-stack GitHub Clone built using the MERN stack (MongoDB, Express.js, React.js, Node.js) that recreates the core features and experience of GitHub.

Secure JWT-based Authentication (Signup, Login, Logout)
Repository Management – Create, edit, delete repositories with descriptions

start command backend = node index.js start
start command backend = npm run dev
